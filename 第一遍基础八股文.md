# C++ 和计算机基础八股文

过第一遍八股文





1. 头⽂件climits定义了符号常ᰁ：例如：INT_MAX 表示 int 的最⼤值，INT_MIN 表示 int 的最⼩值。

2. 静态成员变ᰁ必须在类外部单独定义，以便为其分配存储空间。

3. define和inline: define发生在预处理，inline发生在编译时期，inline是具有类型检查的，把生成的函数体插入到调用的地方，是一种请求，可以不执行；

4. **C++**中inline编译限制：

   \1. 不能存在任何形式的循环语句

   \2. 不能存在过多的条件判断语句

   \3. 函数体不能过于庞⼤

   \4. 内联函数声明必须在调⽤语句之前

5. **new** **和** **malloc**的区别: 返回值，new 失败会报错，而malloc失败返回nullptr, 调用构造函数

6. 为什么static不能是virtual   vbptr vfptr

7. // 下⾯代码在Linux平台上⽆法编译通过（指在gcc编译器）

   std::atomic<int> value = 99;

   // 出错的原因是这⾏代码调⽤的是std::atomic的拷⻉构造函数

   // ⽽根据C++11语⾔规范，std::atomic的拷⻉构造函数使⽤=delete标记禁⽌编译器⾃动⽣成

   // g++在这条规则上遵循了C++11语⾔规范。

8. **struct**和**Class**的区别: 只有数据成员时，用struct;

9. static_cast : 派生类到基类是准确的，基类到派生类是不准确的.  基类到派生类要用dynamic_cast ,const_cast去掉const属性；

10. **内存对⻬是什么？为什么需要考虑内存对⻬？**

内存对齐是值一个变量的地址是某个值/他自身大小的整数倍；

11.  pipe/ socket / sharedmemory / 信号量/ signal / messageQueue

12.   面试题：

    >
    >
    > main thread   |                 child thread
    >
    >new promise
    >
    >get() -> future
    >
    >thread                                -> future
    >
    >set_future(product)         ->  product
    >
    >join()

![image-20250921150035940](C:\Users\guijunliu\AppData\Roaming\Typora\typora-user-images\image-20250921150035940.png)

![image-20250921150159603](C:\Users\guijunliu\AppData\Roaming\Typora\typora-user-images\image-20250921150159603.png)

12 . 菱形继承：

class A: public virtual B

13. 空间配置器
14. 空间配置器中，**大于 1024 字节的内存通常不使用内存池**，而是直接向操作系统申请。这是平衡性能、内存利用率和实现复杂度的最优选择 —— 内存池擅长优化高频、小块的内存操作，而大块内存更适合交给系统直接管理。
15. deque的内部实现：
16. 需要一个中控器数组map，每个迭代器包含几部分:   begin end  node  cur 
17. C++ lambda表达式的实现原理: 编译器会在遇到lambda表达式的时候，会生成匿名的类，然后重载运算符表达式（）；
18. 

   

## 计算机基础篇

重点完成：socket    /   分段



DHCP：



1. DNS解析



TCP:

这⾥是重中之重，可以扩展很多问题，⽐如为什么是三次，不是两次、四次？如果第⼀次握⼿丢失了会发⽣

什么？三次握⼿过程中可以携带数据吗？

![image-20250921170727778](C:\Users\guijunliu\AppData\Roaming\Typora\typora-user-images\image-20250921170727778.png)

递归查询适合普通⽤户和客户端，⽽迭代查询适⽤于DNS服务器之间的通信。



HTTP1.1

1. 长连接
2. **HTTP/1.1** **管道解决了请求的队头阻塞，但是没有解决响应的队头阻塞**。



补充下websocket的内容；

```
Upgrade: websocket;

```

响应状态码： 101；

### HTTPS

1. ssl/TLS的握手过程；
2. CA发布的数字证书；
3. 数字签名和数字证书；
4. 数字签名：私钥加密过的hash值；
5. CSR带着自己的公钥和信息去到CA机构中；





**窗⼝⼤⼩：**

接收窗⼝，告诉对⽅本端TCP缓冲区还有多少空间可以接收数据，⽤来做流量控制。





知：**超时重传时间****RTO****的值 应该略⼤于 报⽂往返****RTT****的值，且是动态变化的。**





子网网络地址+ 子网主机地址，然后把网络划分成很多环境：

1. 测试环境

2. 生产环境

3. 对于⼀对⼀通信的 IPv6 地址，主要划分了三类单播地址，每类地址的有效范围都不同。

   在同⼀链路单播通信，不经过路由器，可以使⽤链路本地单播地址，IPv4 没有此类型

   在内⽹⾥单播通信，可以使⽤唯⼀本地地址，相当于 IPv4 的私有 IP

   在互联⽹通信，可以使⽤全局单播地址，相当于 IPv4 的公有 IP

### 大名鼎鼎的DHCP